{
  "prefix": "tsrafc",
  "body": [
    "export type ${TM_FILENAME_BASE}Props = {}",
    "",
    "export const ${1:${TM_FILENAME_BASE}} = (props: Props) => {",
    "  return (",
    "    <>${1:first}</>",
    "  )",
    "}"
  ],
  "description": "Creates a React Arrow Function Component With TypeScript interface"
}
